import React from 'react';

const SubmitIconButton = ({...props}) => {
    return (
        <button {...props} className='btn btn_submit-status-icon'>
            <svg />
        </button>
    );
};

export default SubmitIconButton;