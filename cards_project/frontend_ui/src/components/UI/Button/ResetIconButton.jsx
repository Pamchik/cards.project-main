import React from 'react';

const ResetIconButton = ({...props}) => {
    return (
        <button {...props} className='btn btn_reset-icon'>
            <svg />
        </button>
    );
};

export default ResetIconButton;